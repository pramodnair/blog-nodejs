<%- include("partials/header") %>
    <h1>Welcome to my daily blog</h1>
    <% posts.map( post=> { %>
        <!-- <div class="card-group w-50 mt-5">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title">
                        <%= post.title %>
                    </h4>
                    <p class="card-text">
                        <%= post.content %>
                    </p>
                </div>
            </div>
        </div> -->

        <div class="row w-50 mt-5">
          <div class="col-sm">
            <div class="card">
              <div class="card-body">
                <h3 class="card-title"><%= post.title %></h3>
                <p class="card-text"><%= post.content %></p>
              </div>
            </div>
          </div>
        </div>
        <% }) %>
<%- include("partials/footer") %>